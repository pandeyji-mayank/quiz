import React from 'react';
import CreateQuiz from '../components/Quiz/CreateQuiz';

const CreateQuizPage = ({ history }) => {
    return <CreateQuiz history={history} />;
};

export default CreateQuizPage;
